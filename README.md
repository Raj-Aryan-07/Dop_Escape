# Dop_Escape
This is a repo for a special extension that can reduce the amount of screen time . Just download these files and store it in a single new folder. Then, on Chrome go to chrome://extensions/ and Turn on the Developer mode toggle in the top-right corner: Click Load unpacked in the top-left corner and select the unzipped folder. 
